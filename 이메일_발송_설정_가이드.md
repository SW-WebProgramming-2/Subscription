# 이메일 발송 기능 설정 가이드

## 📋 단계별 설정 방법

### 1단계: 패키지 설치
터미널에서 다음 명령어를 실행하세요:
```bash
npm install
```

### 2단계: .env.local 파일 설정

프로젝트 루트 폴더(`webproject`)에 `.env.local` 파일을 만들고 아래 내용을 복사해서 넣으세요:

#### Gmail 사용하는 경우:
```env
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=여기에_본인_이메일@gmail.com
SMTP_PASS=여기에_앱_비밀번호
```

#### Gmail 앱 비밀번호 만드는 방법:
1. Google 계정 설정 (https://myaccount.google.com/) 접속
2. 보안 → 2단계 인증 활성화 (필수)
3. 보안 → 앱 비밀번호 클릭
4. "메일"과 "기타(맞춤 이름)" 선택 후 "생성"
5. 생성된 16자리 비밀번호를 복사해서 `SMTP_PASS`에 넣기

#### 네이버 메일 사용하는 경우:
```env
SMTP_HOST=smtp.naver.com
SMTP_PORT=465
SMTP_USER=여기에_본인_이메일@naver.com
SMTP_PASS=여기에_네이버_비밀번호
```

#### Outlook 사용하는 경우:
```env
SMTP_HOST=smtp-mail.outlook.com
SMTP_PORT=587
SMTP_USER=여기에_본인_이메일@outlook.com
SMTP_PASS=여기에_아웃룩_비밀번호
```

### 3단계: 서버 재시작
`.env.local` 파일을 수정한 후에는 **반드시 개발 서버를 재시작**해야 합니다:

1. 현재 실행 중인 서버를 중지 (Ctrl + C)
2. 다시 시작: `npm run dev`

### 4단계: 사용 방법

1. **로그인** 후 **프로필 페이지**로 이동
2. 관리자 계정으로 로그인한 경우:
   - 비밀번호 확인 후 회원 목록이 보임
3. 회원 목록에서 **"조회"** 버튼 클릭
4. 회원 상세 정보에서 **"📧 이메일 알림 보내기"** 버튼 클릭
5. 제목과 메시지를 입력하고 **"발송하기"** 클릭

## ⚠️ 주의사항

- `.env.local` 파일은 절대 Git에 커밋하지 마세요 (이미 .gitignore에 포함되어 있을 것입니다)
- Gmail 사용 시 일반 비밀번호가 아닌 **앱 비밀번호**를 사용해야 합니다
- 이메일 발송 기능은 관리자만 사용할 수 있습니다

## 🐛 문제 해결

### "이메일 서버 설정이 필요합니다" 오류가 나는 경우
- `.env.local` 파일이 프로젝트 루트에 있는지 확인
- 환경 변수 이름이 정확한지 확인 (SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS)
- 서버를 재시작했는지 확인

### "이메일 발송에 실패했습니다" 오류가 나는 경우
- 이메일 주소와 비밀번호가 정확한지 확인
- Gmail 사용 시 앱 비밀번호를 사용했는지 확인
- 방화벽이나 네트워크 설정 확인

