# 협업 가이드 - 금융 API 보안 강화

## 🤝 팀원과 상의하기 전 체크리스트

### ✅ 안전한 변경사항 (즉시 적용 가능)
다음 변경사항은 기존 코드에 영향을 주지 않으므로 바로 적용해도 됩니다:

1. **보안 헤더 추가** - 모든 API 응답에 보안 헤더 추가
2. **Origin 검증** - CORS 공격 방지
3. **요청 크기 제한** - DoS 공격 방지
4. **입력 검증 강화** - 기존 검증 로직 유지하며 추가 검증
5. **에러 처리 개선** - 민감 정보 노출 방지

### ⚠️ 팀원과 상의 필요한 변경사항

#### 1. 토큰 암호화 (선택적 기능)
- **현재 상태**: 비활성화됨 (기존 코드와 호환)
- **활성화 방법**: `ENCRYPT_TOKEN=true` 환경 변수 설정
- **영향**: 클라이언트 코드 수정 필요할 수 있음
- **권장**: 팀원과 상의 후 결정

#### 2. Rate Limiting 값 조정
- 현재 설정:
  - 인증 API: 1분에 5회
  - 콜백 API: 1분에 3회
  - 거래 내역 API: 1분에 20회
- 필요시 팀원과 상의하여 조정 가능

## 📋 Pull Request 작성 시 포함할 내용

### 1. 변경사항 요약
```
금융 API 보안 강화
- 보안 헤더 추가 (XSS, 클릭재킹 방지)
- Origin 검증 추가
- 요청 크기 제한 추가
- 토큰 암호화 기능 추가 (선택적, 기본 비활성화)
```

### 2. 테스트 체크리스트
- [ ] 인증 플로우 정상 작동 확인
- [ ] 콜백 처리 정상 작동 확인
- [ ] 거래 내역 조회 정상 작동 확인
- [ ] 보안 헤더가 응답에 포함되는지 확인
- [ ] Origin 검증이 정상 작동하는지 확인

### 3. 환경 변수 설정 안내
```env
# 필수
OPENBANKING_CLIENT_ID=your_client_id
OPENBANKING_CLIENT_SECRET=your_client_secret

# 권장
ENCRYPTION_KEY=your_encryption_key
ALLOWED_ORIGINS=https://yourdomain.com
ALLOWED_REDIRECT_DOMAINS=yourdomain.com

# 선택사항 (토큰 암호화)
# ENCRYPT_TOKEN=true
```

## 🔄 롤백 계획

문제 발생 시 롤백 방법:

```bash
# 변경 전 커밋 확인
git log --oneline

# 특정 커밋으로 되돌리기
git revert <commit-hash>

# 또는 특정 파일만 되돌리기
git checkout <commit-hash> -- src/app/api/openbanking/auth/route.js
```

## 💬 팀원과 상의할 질문

1. **토큰 암호화 활성화 여부**
   - 현재는 비활성화되어 있어 기존 코드와 호환됩니다
   - 보안을 위해 활성화할지 팀원과 상의 필요

2. **Rate Limiting 값**
   - 현재 설정이 적절한지 확인 필요
   - 사용 패턴에 따라 조정 가능

3. **환경 변수 관리**
   - 프로덕션 환경 변수 설정 방법 확인
   - 보안 키 관리 방법 확인

## 📝 코드 리뷰 시 확인사항

리뷰어가 확인해야 할 사항:

1. ✅ 보안 로직이 올바르게 구현되었는지
2. ✅ 기존 기능에 영향이 없는지
3. ✅ 에러 처리가 적절한지
4. ✅ 로깅에 민감 정보가 노출되지 않는지
5. ✅ 환경 변수 문서화가 되어 있는지

## 🚀 배포 전 체크리스트

- [ ] 모든 환경 변수 설정 완료
- [ ] 테스트 환경에서 검증 완료
- [ ] 팀원 코드 리뷰 완료
- [ ] 문서 업데이트 완료 (SECURITY_CHANGES.md)
- [ ] 롤백 계획 수립 완료

## 📞 문의

변경사항에 대한 질문이나 문제가 있으면:
1. 이슈 생성
2. 팀 채팅방에 공유
3. 코드 리뷰 요청

